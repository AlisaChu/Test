package org.example.less17;

public class MatrixCal {
    public boolean MatrixCal(int[][] array) {
        if (array.length == array[0].length) {
            return true;
        } else {
            return false;
        }
    }
}
