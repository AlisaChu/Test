package org.example.less18;

public class Matrix {
    public boolean matrixSquare(int[][] array) {
            if (array.length == array[0].length) {
                return true;
            } else {
                return false;
            }
        }
    }
